<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Blogging App" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>




    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <a class="navbar-brand" href="\">Blogging App</a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "Posts", posts_path  %></li>
            <li><%= link_to "Users", users_path  %></li>
            <li><%= link_to "Hashtags", "/path3"  %></li>
            <li class="float-right">
              <% if logged_in? %>
              <div class="nav-user-logged_in">
                <%= link_to controller: "users", action: "show", id: current_user.id do %>
                <%= image_tag (current_user.picture("small")) %> <%= current_user.name %>
                <% end %>
                (<%= link_to "logout",logout_path,class: "button_1" %>)
              </div>

              <% else %>
              <%= link_to "/auth/facebook", {class:"fb-login-button"} do %>
                                                  <i class="fa fa-facebook-square"></i> Login with facebook
                                                  <% end %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2019</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
